rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow all authenticated users to read, write, and delete any file
    match /{allPaths=**} {
      allow read, write, delete: if request.auth != null;
    }
  }
}
